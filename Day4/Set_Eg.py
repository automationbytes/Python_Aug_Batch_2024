'''
Set used to store the values without duplicates
Insertion order will not be preserved (Cant maintain the insertion order)
It is mutable (Modifiable)
Set will not allow indexing (unindexed)
Hetro - similar and diff data types

{} - set

'''

fruits = {"Apple","Banana","Grapes","Mango","Apple"}
print(fruits)
print(type(fruits))

print(len(fruits))

for f in fruits:
    print(f)